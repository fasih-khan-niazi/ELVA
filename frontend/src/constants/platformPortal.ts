/** localStorage backups while an operator swaps into sandbox tenant JWT exploration. */

export const PLATFORM_JWT_BACKUP_KEY = 'elva_platform_backup_jwt';

export const PLATFORM_USER_BACKUP_KEY = 'elva_platform_backup_user';
